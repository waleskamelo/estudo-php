<?php


	//Operadores de comparação
	// == Igual
	// === Identido (tipo e valor devem ser iguais)
	// != Diferente
	// !== Não idêntico (tipo e valor devem ser diferentes)
	// < Menor
	// > Maior
	// <= Menor ou igual
	// >= Maior ou igual

	//Operadores lógicos
	//&& ou AND -> conecta duas expressões, retorna verdadeiro se todos os resultados das expressões forem verdade.
	//|| ou OR -> Retorna verdadeiro se uma das expressões forem verdadeiras.
	//XOR -> Retrna verdadeiro apenas se 1 das expressões for verdade.
	//! -> Inverter o resultado.
	//() -> Cria precedência na execução do código. Dá prioiridade e sentido a expressões mais detalhadas.


	if(5 == 3){
		echo 'verdadeiro';
	} else{
		echo 'Entrou no bloco else';
	}

?>